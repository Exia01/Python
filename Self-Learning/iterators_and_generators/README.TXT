Iterator and Iterables

Iterator - An Object that can be iterated upon. 
An object which returns data, one element at a time when next() is called on iterated

Iterables - an object will return an iterator when iter() is called on it. 

